// Generated by CoffeeScript 1.6.3
(function() {
  Denigma.Graph = (function() {
    function Graph() {}

    Graph.sig = null;

    Graph.cont = null;

    Graph.prototype.init = function() {
      this.cont = document.getElementById("graph");
      this.sig = sigma.init(this.cont);
      return this.sig.drawingProperties({
        defaultLabelColor: "#ccc",
        font: "Arial",
        edgeColor: "source",
        defaultEdgeType: "curve"
      }).graphProperties({
        minNodeSize: 1,
        maxNodeSize: 5
      });
    };

    Graph.prototype.add = function() {};

    Graph.prototype.test = function() {
      var hello, world;
      hello = this.sig.addNode("hello", {
        label: "Hello",
        x: Math.random(),
        y: Math.random()
      });
      world = this.sig.addNode("world", {
        label: "World!",
        x: Math.random(),
        y: Math.random()
      });
      return this.sig.addEdge('hello_world', 'hello', 'world').draw();
    };

    return Graph;

  })();

}).call(this);

/*
//@ sourceMappingURL=graph.map
*/
