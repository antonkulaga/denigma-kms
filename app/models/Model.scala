package models

import org.denigma.macroses._

import org.denigma.macroses.Macros
import com.tinkerpop.blueprints.Direction
import scala.collection.immutable.Map
import java.util.UUID

abstract class Model extends ModelLike{
//  var id = ""
//  def getId:String = if(id=="") {id = UUID.randomUUID().toString; id} else id



 // case class Link(label:String,from:String,to:String,props:Map[String,Any]=Map.empty[String,Any],types:Seq[String]=List.empty[String], id:String="") extends LinkLike



}